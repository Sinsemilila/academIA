node_modules/
__pycache__/
*.pyc
dist/
.next/
.svelte-kit/
.env
.env.glitchtip
.env.glitchtip.sops
*.log
*.bak
.dify_admin_key
venv/
.venv/
# Plaintext LiteLLM config must never be committed (contains OpenAI key).
# Encrypted sops file + wrapper are tracked explicitly under litellm/ subdir.
litellm/config.yaml
litellm/config.yaml.backup*
.agent

# Phase 6 Sprint Oracle EN cohérence — verdict cache (gitignored, local dev only)
scripts/oracle/.cache/

# S56 oracle/ tooling venv (ruff + mypy, local dev only)
scripts/oracle/.venv-tools/
.mypy_cache/
.ruff_cache/
